# Repository Inventory

Reviewed 2026-02-14.

## Crate and binaries

- Rust crate: `brief-bright-gone`, edition 2024, version 0.1.0.
- Library modules: `src/lib.rs`, `src/detect.rs`, `src/safety.rs`, and `src/normalize.rs`.
- Binaries: `src/bin/bbg.rs` and `src/bin/bbg-proxy.rs`.
- Build and test commands: `cargo build` and `cargo test`.

## Tests and fixtures

- No standalone `tests/` or `benches/` directory exists yet.
- Unit-test coverage is currently embedded in the source modules; the fixture/property-test scaffolding remains future work.

## License material

- Package metadata is in `Cargo.toml`.
- Repository license text is in `LICENSE`.
- User-facing license mention is in `README.md`.
- The project license is Apache-2.0.
